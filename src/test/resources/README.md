This directory holds test resources used by automated tests (e.g., feature files, test data, test configuration files).

Place your resource files here under `src/test/resources` so they are available on the test classpath.
